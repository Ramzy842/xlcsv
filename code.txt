for (let x = 0; e.dataTransfer.items[x]; x++) {
            console.log("files:\n", e.dataTransfer.items[x].webkitGetAsEntry());
            if (e.dataTransfer.items[x].webkitGetAsEntry().isDirectory) {
                // console.log("directory");
                let directoryReader = e.dataTransfer.items[x].webkitGetAsEntry().createReader();
                // console.log(directoryReader);
                directoryReader.readEntries((entries) => {
                  entries.forEach((entry) => {
                    // scanFiles(entry, directoryContainer);
                    console.log(entry);
                  });
                });
              }
        }